<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MyConference.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MyConference"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Title="Schedule">
            <ShellContent
                Title="Day 1"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="ScheduleDay1" />
            <ShellContent
                Title="Day 2"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="ScheduleDay2" />
        </Tab>
        <Tab Title="My Agenda">
            <ShellContent
                Title="Day 1"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MyAgendaDay1" />

            <ShellContent
                Title="Day 2"
                ContentTemplate="{DataTemplate  local:MainPage}"
                Route="MyAgendaDay2" />
        </Tab>
        <Tab Title="Sponsors">
            <ShellContent
                Title="Sponsors"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="Sponsors" />
        </Tab>
    </TabBar>
    
</Shell>
